{"date": "2026-08-25", "failure": "no-fire", "prompt": "/benchmark-harness invoked in pi", "expected": "run preflight + detection, print the plan, wait at the confirm gate", "actual": "agent reasoned 'the user hasn't explicitly asked yet' and replied that it was ready when they were, doing nothing", "note": "pi injects the SKILL.md body without a user turn attached; the body's 'run only when the user explicitly invokes' guard read as 'nobody invoked'. Fixed in 1.1.0 by the Start here section."}
