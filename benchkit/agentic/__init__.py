"""Agentic tool-calling suite: a sandboxed workspace, tool schemas, and the agent loop.

Unlike the one-shot suites, a task here is solved only when a predicate over the
final workspace says so — see benchkit/agentic/tasks.py.
"""
